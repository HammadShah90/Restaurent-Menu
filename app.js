var mainBtns = ['All', 'Breakfast', 'Lunch', 'Shakes', 'Dinner'];

var mainButtons = document.querySelector(".mainButtons");

console.log(mainButtons);

var mainButtonContent = []

mainBtns.forEach((btn) => {
    var string = '<button>All</button>';
})